# Домашнее задание к занятию «RabbitMQ»

## Задание 1. Установка RabbitMQ

Для выполнения задания я установил и запустил `RabbitMQ` на виртуальной машине Ubuntu.

После установки включил management plugin командой:

```bash
sudo rabbitmq-plugins enable rabbitmq_management
```
![rabbitmq-ui](rabbitmq-ui.png)
___

## Задание 2. Отправка и получение сообщений

Для выполнения задания я установил Python-библиотеку pika и подготовил скрипты producer.py и consumer.py.

После этого выполнил тестовую отправку сообщения через producer.py. В результате в веб-интерфейсе RabbitMQ появилась очередь hello.

Затем запустил consumer.py и убедился, что сообщение успешно получено.

Скриншот очереди

Скриншот получения сообщения

____

## Задание 3. Подготовка HA кластера

Для выполнения задания я создал вторую виртуальную машину, установил на нее RabbitMQ и настроил обмен именами между машинами через файл /etc/hosts.

После этого я объединил обе ноды в один кластер и проверил его состояние.

Скриншоты вывода rabbitmqctl cluster_status

Далее в веб-интерфейсе RabbitMQ проверил, что в кластере доступны обе ноды.

Скриншот нод кластера

Так как использовалась версия RabbitMQ 4.2, вместо устаревшей политики ha-all была использована современная quorum queue hello.

После этого я проверил, что очередь hello создана как quorum queue.

Скриншот quorum queue

Затем с помощью rabbitmqadmin убедился, что сообщение в очереди доступно.

Скриншот результата rabbitmqadmin get queue=hello

Вывод

В ходе выполнения домашнего задания я установил и настроил RabbitMQ, включил management plugin и проверил работу веб-интерфейса.

Далее я выполнил тестовую отправку и получение сообщения с помощью Python-скриптов producer.py и consumer.py, а также библиотеки pika. В результате была создана очередь hello, и я убедился, что сообщения успешно отправляются и получаются.

После этого я подготовил кластер RabbitMQ из двух нод, настроил обмен именами через файл /etc/hosts, привел обе машины к единому Erlang cookie и объединил их в один кластер.

Так как использовалась версия RabbitMQ 4.2, вместо устаревшей политики ha-all была использована современная quorum queue hello.

В результате я на практике посмотрел:

установку и базовую настройку RabbitMQ;
работу с очередями и обменом сообщениями через producer/consumer;
объединение двух нод в кластер и работу очереди в кластерной конфигурации.
